interface:
  display_name: "Migrate to SolidRPC"
  short_description: "Move an EVM app to one SolidRPC integration"
  default_prompt: "Use $migrate-to-solidrpc to add SolidRPC to this EVM application while preserving its current production RPC route."
policy:
  allow_implicit_invocation: true
